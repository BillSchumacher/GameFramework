namespace GameFramework.UI
{
    public enum AnchorPoint
    {
        Manual,     // Position is set directly using X, Y (absolute)
        TopLeft,
        TopCenter,
        TopRight,
        MiddleLeft,
        MiddleCenter,
        MiddleRight,
        BottomLeft,
        BottomCenter,
        BottomRight
    }
}
