namespace GameFramework
{
    public abstract class BaseAction
    {
        public abstract string Name { get; }
        // Base properties or methods for actions can be defined here
        // For now, it can be an empty class to serve as a base type
    }
}
